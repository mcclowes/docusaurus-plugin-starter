---
title: Using global data
---

# Using global data

The starter demonstrates how to share data with the client bundle via `setGlobalData`. Once the
plugin runs, the `StarterMessage` theme component can read the greeting defined in
`examples/docusaurus-v3/docusaurus.config.js`.

NOTE: Try changing the greeting in the config, then refresh this page. The message updates without
touching any content files – a common pattern for Docusaurus plugins.
